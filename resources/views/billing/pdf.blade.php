<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans;font-size:11px}h1{color:#0d6efd}table{width:100%;border-collapse:collapse}th{background:#0d6efd;color:#fff}th,td{padding:7px;border:1px solid #ddd}</style></head><body><h1>Dentaris</h1><h2>Factura {{ $invoice->invoice_number }}</h2><p>Paciente: {{ $invoice->patient->first_name ?? '' }} {{ $invoice->patient->last_name ?? '' }}</p><table><thead><tr><th>Concepto</th><th>Cant.</th><th>Precio</th><th>Total</th></tr></thead><tbody>@foreach($invoice->items as $item)<tr><td>{{ $item->item_name ?: ($item->cdtCatalog->name ?? '') }}</td><td>{{ $item->quantity }}</td><td>${{ number_format($item->unit_price,2) }}</td><td>${{ number_format($item->total_price,2) }}</td></tr>@endforeach</tbody></table><h3>Total: ${{ number_format($invoice->total_amount,2) }}</h3></body></html>
